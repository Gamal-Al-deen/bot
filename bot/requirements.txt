# المتطلبات الأساسية لتشغيل البوت
# Requirements for running the bot

# مكتبة requests للاتصال بـ API
requests==2.31.0

# Flask لخادم Webhook (مطلوب لـ Render)
flask==3.0.0

# Supabase Database (مطلوب لقاعدة البيانات)
# Updated to latest stable version to fix proxy parameter error
supabase>=2.10.0

# لا حاجة لمكتبات إضافية
# No additional libraries needed
